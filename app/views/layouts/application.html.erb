<!DOCTYPE html>
<html>
  <head>
    <title>Tasker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if current_user %>
      <header class="container mx-auto mt-3 px-5 flex justify-between items-center">
        <div>Hello, <%= current_user %></div>
        <nav class="">
          <%= button_to 'Sign out', destroy_user_session_path, method: :delete, class: "btn-secondary" %>
        </nav>
      </header>
      <hr class="mt-3" />
    <% end %>
    <main class="container mx-auto mt-16 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
